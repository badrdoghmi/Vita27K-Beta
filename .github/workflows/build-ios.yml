name: Build iOS IPA

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-14
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        submodules: recursive
        
    - name: Install dependencies via Homebrew
      run: |
        brew install boost cmake ninja
        
    - name: Set up MoltenVK
      run: |
        git clone https://github.com/KhronosGroup/MoltenVK.git
        cd MoltenVK
        make ios
        # Find and copy framework to predictable location
        FRAMEWORK_PATH=$(find . -name "MoltenVK.framework" -type d | head -n 1)
        mkdir -p ${{ github.workspace }}/mvk-out
        cp -R "$FRAMEWORK_PATH" ${{ github.workspace }}/mvk-out/
        
    - name: Configure CMake
      run: |
        mkdir -p build-ios
        cd build-ios
        cmake .. -G Xcode \
          -DCMAKE_SYSTEM_NAME=iOS \
          -DCMAKE_OSX_SYSROOT=iphoneos \
          -DCMAKE_OSX_ARCHITECTURES="arm64" \
          -DVITA3K_FORCE_PORTABLE=ON \
          -DMOLTENVK_FRAMEWORK="${{ github.workspace }}/mvk-out/MoltenVK.framework" \
          -DBoost_ROOT=$(brew --prefix boost) \
          -DBoost_NO_SYSTEM_PATHS=ON \
          -DBoost_NO_BOOST_CMAKE=ON
        
    - name: Build Xcode project
      run: |
        cd build-ios
        xcodebuild -project Vita3K.xcodeproj \
          -scheme Vita3K \
          -configuration Release \
          -sdk iphoneos \
          CODE_SIGNING_ALLOWED=NO \
          GCC_GENERATE_DEBUGGING_SYMBOLS=NO \
          OTHER_CFLAGS="-O3" \
          OTHER_CPLUSPLUSFLAGS="-O3" \
          build
        
    - name: Create IPA
      run: |
        mkdir -p Payload
        cp -R build-ios/Release-iphoneos/Vita3K.app Payload/
        zip -r Vita3K.ipa Payload
        
    - name: Upload IPA artifact
      uses: actions/upload-artifact@v4
      with:
        name: Vita3K-iOS-IPA
        path: Vita3K.ipa
